<Window x:Class="ToolsFramework.Host.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ToolsFramework.Host.ViewModels"
        Title="Game Tools Framework — Plugin Host" 
        Height="800" Width="1300" MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen" Background="#0D1117">

    <Window.DataContext>
        <vm:HostViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="180"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- left sidebar - plugin list -->
        <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Background="#161B22"
                BorderBrush="#30363D" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- app title -->
                <Border Grid.Row="0" Background="#0D1117" Padding="16,12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔧" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Game Tools" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                        </StackPanel>
                        <TextBlock Text="Plugin Framework" Foreground="#8B949E" FontSize="11" Margin="28,2,0,0"/>
                    </StackPanel>
                </Border>

                <!-- refresh button -->
                <Button Grid.Row="1" Content="🔄 Reload Plugins" Command="{Binding RefreshPluginsCommand}"
                        Background="#21262D" Foreground="#E6EDF3" BorderThickness="0" Padding="12,8"
                        Margin="12,8" Cursor="Hand" FontSize="12"/>

                <!-- plugin list -->
                <ListBox Grid.Row="2" ItemsSource="{Binding Plugins}" SelectedItem="{Binding SelectedPlugin}"
                         Background="Transparent" BorderThickness="0" Foreground="#E6EDF3" Margin="4,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10,8" Margin="2" CornerRadius="6" Background="#21262D"
                                    Cursor="Hand">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Icon}" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="#E6EDF3"/>
                                            <TextBlock Text="{Binding Version}" FontSize="10" Foreground="#8B949E"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#8B949E" 
                                               TextWrapping="Wrap" Margin="26,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- main content ie active plugin view -->
        <Border Grid.Column="1" Grid.Row="0" Margin="0">
            <Grid>
                <!-- no plugin selected message -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding HasPluginSelected, Converter={StaticResource BoolToVis}, 
                                        ConverterParameter=Inverse, FallbackValue=Visible}">
                    <TextBlock Text="🔧" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock Text="Select a plugin from the sidebar" FontSize="18" 
                               Foreground="#8B949E" HorizontalAlignment="Center"/>
                    <TextBlock Text="Plugins are loaded dynamically from the Plugins folder"
                               FontSize="12" Foreground="#484F58" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- plugin content area -->
                <ContentControl Content="{Binding CurrentPluginView}"
                                Visibility="{Binding HasPluginSelected, Converter={StaticResource BoolToVis}}"/>
            </Grid>
        </Border>

        <!-- this is for the log panel -->
        <Border Grid.Column="1" Grid.Row="1" Background="#161B22" 
                BorderBrush="#30363D" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="📋 Activity Log" FontSize="12" FontWeight="SemiBold"
                           Foreground="#8B949E" Margin="12,8"/>

                <ListView Grid.Row="1" ItemsSource="{Binding LogEntries}" Background="Transparent"
                          BorderThickness="0" Foreground="#E6EDF3" Margin="4,0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="" Width="30">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Icon}" FontSize="12"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Time" Width="70">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}" 
                                                   Foreground="#484F58" FontSize="11" FontFamily="Consolas"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Source" Width="120">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Source}" Foreground="#58A6FF" FontSize="11"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Message" Width="600">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Message}" Foreground="#E6EDF3" FontSize="11"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>

        <!-- status bar section -->
        <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" Background="#0D1117" 
                BorderBrush="#30363D" BorderThickness="0,1,0,0" Padding="12,6">
            <TextBlock Text="{Binding StatusText}" Foreground="#8B949E" FontSize="11"/>
        </Border>
    </Grid>
</Window>